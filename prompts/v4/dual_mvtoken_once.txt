You are controlling a dual-arm robot with three cameras:
- Agentview: first-person view of the workspace and both arms
- Wristleft: close-up view from the LEFT gripper
- Wristright: close-up view from the RIGHT gripper

Task: {task}
Left arm recent moves, newest first: {recent_left}
Right arm recent moves, newest first: {recent_right}

Output exactly two action tokens separated by a space: the LEFT arm token first, then the RIGHT arm token.
Each token is one of:
MV_FWD, MV_BACK, MV_LEFT, MV_RIGHT, MV_UP, MV_DOWN, GRASP, RELEASE, STILL, DONE

Check all three camera views and choose the next action for each arm:
- Use Agentview to locate the target and to keep the two arms coordinated
- Use each wrist view to fine-align that arm when the target is visible up close
- GRASP when that gripper's fingers are aligned around the target
- RELEASE when that gripper holds the target above its destination
- STILL when that arm must hold its position while the other arm catches up
- DONE DONE when the whole task is complete and both grippers are clear
- Avoid repeating a direction that conflicts with that arm's most recent move

Return the two tokens only, no punctuation, no explanation:
